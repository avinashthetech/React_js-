import { createRoot } from "react-dom/client";
import App from "./App";

// This code selects the 'root' element in the HTML document and renders the App component inside it.
createRoot(document.getElementById('root')).render(<App />);
